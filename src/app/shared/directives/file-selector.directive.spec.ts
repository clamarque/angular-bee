import { FileSelectorDirective } from './file-selector.directive';

describe('FileSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new FileSelectorDirective();
    expect(directive).toBeTruthy();
  });
});
